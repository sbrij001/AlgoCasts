// accepts an arg of an array full of nums and an arg of a value
// loop thru the entire array
// check if the current ele is equal to the value
// if it is return the index at which it is found
// if it is never found return -1

function findNum(array, value){
  for(let i = 0; - < array.length; i++){
    if (array[i] === value) {
      return i;
    }
  }
};
